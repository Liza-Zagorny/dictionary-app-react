import React from "react";
import "../css/Phonetic.css";

function Phonetic(props) {
  console.log(props);
  //   for (const [key, value] of Object.entries(props.phonetic)) {
  //     return `${key}: ${value}`;
  //   }
  return <ul></ul>;
}
export default Phonetic;
